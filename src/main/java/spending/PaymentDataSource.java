package spending;

import java.util.ArrayList;
import java.util.List;

public class PaymentDataSource {
    public static List<Payment> fetch(long userId, int year, int month) {
        List<Payment> payments = new ArrayList<Payment>();

        return payments;
    }
}
